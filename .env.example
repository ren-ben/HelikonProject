# Copy this file to .env and fill in the values.
# .env is gitignored — never commit it.

# ─── Database ─────────────────────────────────────────────────
POSTGRES_DB=clil_db
POSTGRES_USER=clil_user
POSTGRES_PASSWORD=clil_pass
POSTGRES_PORT=5432

# ─── LLM / Embedding — cloud API keys ────────────────────────
# LLM_PROVIDER   openai  →  uses ChatOpenAI  +  OpenAIEmbeddings
#                groq    →  uses ChatGroq    +  OpenAIEmbeddings
#                ollama  →  (future) uses ChatOllama + local embeddings
LLM_PROVIDER=openai
LLM_API_KEY=sk-...
LLM_MODEL=gpt-4o-mini
# OpenAI models:  gpt-4o-mini | gpt-4o | gpt-3.5-turbo
# Groq   models:  llama3-8b-8192 | llama3-70b-8192 | mixtral-8x7b-32768
EMBEDDING_API_KEY=sk-...
# OpenAI: same key as LLM_API_KEY.  Model used: text-embedding-3-small

# ─── JWT — auth (Phase 3) ────────────────────────────────────
# Uncomment and fill when implementing authentication.
# JWT_SECRET=replace-with-a-long-random-string-at-least-32-chars
# JWT_EXPIRATION_MS=3600000
# JWT_REFRESH_EXPIRATION_MS=604800000
